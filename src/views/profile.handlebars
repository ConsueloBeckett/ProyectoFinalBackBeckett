<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Profile</title>
    <link rel="stylesheet" type="text/css" href="/css/style.css">
</head>

<body>

  <main>
  <div class="user-profile">
    <h1>User Profile</h1>
    <p>User Type: {{user.role}}</p>
    <p>Email: {{user.email}}</p>
    <p>ID: {{user.id}}</p>
    <div class="button-container">
      <a href="/api/users/premium/{{user.id}}" class="button">Upgrade to Premium</a>

      {{#if isAuthorized}}
        <a href="/addProducts" class="button">Create Product</a>
        <a href="/api/products/manageProducts" class="button">Manage Products</a>
       
      {{/if}}
      {{#if isAdmin}}
        <a href="/api/users/allusers" class="button">All Users</a>
      {{/if}}
    </div>
  </div>
</main>


</body>

</html>
